package com.trace.model;

/**
 * Side of an input-component's body where its output pin(s) exit.
 * Used by {@link InputComponent} and its subclasses to reconfigure pin
 * positions from the Properties panel.
 */
public enum PinSide {
    TOP,
    BOTTOM,
    LEFT,
    RIGHT
}
